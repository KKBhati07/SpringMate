<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- ===== Log directory ===== -->
    <property name="LOG_PATH" value="logs"/>

    <!-- ===== Console Appender (SYNC) ===== -->
    <appender name="CONSOLE"
              class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>
                %d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread]
                %logger{36} [reqId=%X{requestId}] - %msg%n
            </pattern>

        </encoder>
    </appender>

    <!-- ===== File Appender ===== -->
    <appender name="FILE"
              class="ch.qos.logback.core.rolling.RollingFileAppender">

        <!-- Current log file -->
        <file>${LOG_PATH}/application.log</file>

        <!-- Daily rolling policy -->
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>
                ${LOG_PATH}/application-%d{yyyy-MM-dd}.log
            </fileNamePattern>

            <maxHistory>60</maxHistory>

            <totalSizeCap>2GB</totalSizeCap>
        </rollingPolicy>

        <encoder>
            <pattern>
                %d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread]
                %logger{36} [reqId=%X{requestId}] - %msg%n
            </pattern>

        </encoder>
    </appender>

    <!-- ===== Async Wrapper for File Appender ===== -->
    <appender name="ASYNC_FILE"
              class="ch.qos.logback.classic.AsyncAppender">

        <!-- Queue size for buffering logs -->
        <queueSize>5000</queueSize>

        <!-- Never discard logs -->
        <discardingThreshold>0</discardingThreshold>

        <!-- Do not include caller data (faster) -->
        <includeCallerData>false</includeCallerData>

        <appender-ref ref="FILE"/>
    </appender>

    <!-- ===== Root Logger ===== -->
    <root level="INFO">
        <!-- SYNC console logging -->
        <appender-ref ref="CONSOLE"/>

        <!-- ASYNC file logging -->
        <appender-ref ref="ASYNC_FILE"/>
    </root>

</configuration>
