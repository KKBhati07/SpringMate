<configuration>

    <appender name="rolling_file_appender" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>
            testing_rolling.log
        </file>
        <rolling-policy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy" >
            <fileNamePattern>testing-%d{YY-MM-DD}.%i.log</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>10</maxHistory>

        </rolling-policy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="console_appender" class="ch.qos.logback.core.ConsoleAppender" >
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="file_appender" class="ch.qos.logback.core.FileAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n</pattern>
        </encoder>
        <file>
            testing.log
        </file>
    </appender>

    <root level="INFO" >
        <appender-ref ref="console_appender" />
        <appender-ref ref="file_appender" />

    </root>

</configuration>